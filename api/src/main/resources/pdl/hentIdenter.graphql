query($ident: ID!) {
    hentIdenter(ident: $ident, historikk: false, grupper: [FOLKEREGISTERIDENT, AKTORID]) {
        identer {
            ident,
            historisk,
            gruppe
        }
    }}
