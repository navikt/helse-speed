query ($ident: ID!, $historikk: Boolean!) {
    hentIdenter(
        ident: $ident
        historikk: $historikk
        grupper: [FOLKEREGISTERIDENT, AKTORID, NPID]
    ) {
        identer {
            ident
            historisk
            gruppe
        }
    }
}
